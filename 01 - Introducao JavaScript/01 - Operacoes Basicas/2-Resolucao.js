/* Utilize if/else para escrever um código que imprima o maior de dois números.
Defina, no começo do seu código, duas variáveis com os valores que serão comparados. */

const num1 = 5;
const num2 = 5;

if (num1 > num2) {
    console.log(num1);
} else if (num1 === num2) {
    console.log("Os números são iguais");
} else {
    console.log(num2);
}
